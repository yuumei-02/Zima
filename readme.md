# About
Zima is a python flavoured, systems level, programming language that is native and simple.
Zima only targets the x86-64 CPU architecture and for now only supports the Linux kernel as an OS target.

